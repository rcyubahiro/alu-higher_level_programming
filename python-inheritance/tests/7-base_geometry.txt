from your_module_name import BaseGeometry  # Replace 'your_module_name' with the actual module name

# Create an instance of BaseGeometry
bg = BaseGeometry()

# Test instantiation
print(type(bg) == BaseGeometry)  # Expected output: True

# Test area() method (should raise an exception)
try:
    bg.area()
except Exception as e:
    print(e)  # Expected output: "area() is not implemented"

# Test integer_validator()
try:
    bg.integer_validator("my_int", 12)  # Expected output: No error
    print("integer_validator passed")
except Exception as e:
    print(e)

# Test invalid cases for integer_validator()
invalid_values = [0, -4, "4", (4,), [3], True, {3, 4}, None]
for value in invalid_values:
    try:
        bg.integer_validator("age", value)
    except Exception as e:
        print(e)  # Expected: Corresponding error messages

